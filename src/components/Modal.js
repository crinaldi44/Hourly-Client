import React from "react";


/**
 * A modal is a UI component that generally represents an interactable
 * screen consisting of dialog as well as options.
 * @return {JSX.Element}
 */
export const Modal = () => {
    return (
        <>

        </>
    )
}